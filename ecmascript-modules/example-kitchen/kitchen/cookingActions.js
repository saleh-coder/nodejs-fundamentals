// Functions to describe cooking actions for various types of food.

function cut(food) {
  return `cutting ${food}`;
}

function serve(food) {
  return `serving ${food}`;
}

export default { cut, serve };
